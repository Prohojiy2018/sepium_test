1. что было исправлено (pull-request https://github.com/Prohojiy2018/sepium_test/pull/1/changes):

-В PHP: логика вывода и фильтрациив частности, уязвимость XSS, пропажа общих характеристик при выборе категорий и ложные срабатывания (когда ID 1 находился внутри 10). Обернул весь текстовый вывод из БД в htmlspecialchars. Отказался от SQL-поиска LIKE '%ID%'. Теперь скрипт разбивает строку категорий в массив (explode) и ищет точное совпадение (in_array). Добавил жесткое условие: характеристики с пустым cat_prop (общие) выводятся всегда.

-в js:
В property.js поправил сброс введённых значений. При выборе новой категории форма полностью перерисовывалась, и весь введённый пользователем текст стирался. Написал логику, которая перед AJAX-запросом сохраняет текущие значения всех заполненных полей в память (JS-объект), а после получения нового HTML с сервера — автоматически вставляет их обратно в нужные поля.

В main.js характеристики с мультивыбором (чекбоксы) вообще не собирались, а число 0 считалось пустым полем и игнорировалось. Добавил парсинг чекбоксов (:checked) и объединение их значений (ID) в строку через разделитель :::. Изменил проверку на пустоту: вместо if (value) (которая отбрасывает ноль) поставил строгую if (value !== '' && value !== null && value !== undefined).

2. Заняло где-то час
3. Gemini